import React from 'react';

import classes from './PanelVenta.module.css';

const PanelVenta = (props) => {
    return (
        <div className={classes.PanelVenta}>
            Contenido
        </div>
    );
}

export default PanelVenta;