import React from 'react';

export const exercisesContext = React.createContext([]);
export const musclesContext = React.createContext([]);
export const selectedMuscleInTab = React.createContext('');
export const selectedTheme = React.createContext('light');